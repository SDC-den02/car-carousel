import React from 'react';

export const Slides = ({ images }) => {

  return (
    <div className="slider">
      {/* <img src={image.image} key={image.id} className="slider-images" alt="" /> */}
      {/* <span style={styles}/> */}
    </div>
  );
}
